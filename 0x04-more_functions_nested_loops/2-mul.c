#include "main.h"
/**
 * mul - function checks for a digit (0 through).
 * @a: a - variable
 * @b: b - variable
 * Return: always 0.
 */
int mul(int a, int b)
{
	int mul;

	mul = a * b;
	return (mul);
}
